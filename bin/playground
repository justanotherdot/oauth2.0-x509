#!/bin/sh -eu

BASE_URL="http://localhost:3000"

curl -c ./cookies -sL "${BASE_URL}/login" >/dev/null
curl -b ./cookies -H "Authorization: Bearer $(grep access_token ./cookies | cut -f7)" "${BASE_URL}/userinfo"
